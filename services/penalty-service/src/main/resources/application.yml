spring:
  application:
    name: penalty-service
  datasource:
    url: jdbc:postgresql://localhost:5435/blps_penalty
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8084
